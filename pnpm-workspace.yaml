packages:
  - "apps/web"

# pnpm 11 requires an explicit decision for dependencies with postinstall build
# scripts; an undecided one fails `pnpm install` (locally and on Vercel).
allowBuilds:
  esbuild: true
  sharp: false
  unrs-resolver: true
