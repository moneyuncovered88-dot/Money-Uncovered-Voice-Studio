# pnpm uses a flat, hoisted node_modules (no symlinks).
# This avoids EPERM symlink errors on Windows when Developer Mode / admin is off.
node-linker=hoisted
# Keep peer-dependency resolution lenient for the fast-moving Next/React ecosystem.
strict-peer-dependencies=false
